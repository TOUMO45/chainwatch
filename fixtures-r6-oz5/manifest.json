[
  {
    "id": "N6-oz5-01",
    "rule": 6,
    "sub_rule": null,
    "label": "negative",
    "exclusion_ref": "RC-OZ5-R6",
    "path": "negative/N6-oz5-01",
    "source_path": "contracts/RateLimited.sol",
    "remaps": "oz5",
    "solc": "0.8.27",
    "summary": "OZ5 ERC-7201 namespaced-storage: rate-limit require on rotateGuardian(newGuardian) reads block.timestamp + $.lastRotation only (no parameter). Slither's is_dependent($, newGuardian) is spuriously True on the assembly-assigned $ local, so Rule 6 fires today. EXPECTED: quiet after RC-OZ5-R6 fix that requires a real read path from guard to parameter. Locks RC-OZ5-R6."
  },
  {
    "id": "P6-oz5-01",
    "rule": 6,
    "sub_rule": null,
    "label": "positive",
    "exclusion_ref": null,
    "path": "positive/P6-oz5-01",
    "source_path": "contracts/Cap.sol",
    "remaps": "oz5",
    "solc": "0.8.27",
    "summary": "OZ5 ERC-7201 namespaced-storage: setAmount(newAmount) guard `require(newAmount <= $.maxAmount)` genuinely reads newAmount directly plus a namespaced state member via $. Guard removed at N. EXPECTED: fires both before and after the RC-OZ5-R6 fix -- newAmount appears directly in the guard's reads, so the tightened discriminator preserves the fire. Precision-first pair for N6-oz5-01."
  }
]
